allOf:
  - $ref: './BaseOrderBy.yaml'
  - in: query
    default: created_at
    enum:
      - created_at
      - updated_at
      - rating
      - votes_count
      - last_activity_date
      - answers_count
      - view_counter
